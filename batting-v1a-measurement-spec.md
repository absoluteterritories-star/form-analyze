# 打撃動作解析システム Ver.1A 測定項目仕様書 Ver.1.0
### 設計仕様書 Ver.1.0 §20「次工程」に対応する詳細仕様（未決事項なし）

本書は設計仕様書 §10 で「残す」に分類された Ver.1A のコア指標について、
**定義・計算方法・単位・保存する数値・測定信頼度の算出方法**を実装可能な粒度まで確定する。

投球版（`pitching-v1-measurement-spec.md`）と共通にできる部分は共通にし、
**打撃で事情が異なる部分だけを作り直した**。異なる箇所には理由を明記した。

**本書に未決事項はない。この内容で実装に着手できる。**

---

## 1. 決定事項一覧（判断が必要だった点と、その理由）

| # | 論点 | **決定** | 理由 |
|---|---|---|---|
| 1 | 側面カメラの位置 | **投手－捕手を結ぶ線に対して直角。打者の正面側（右打ちなら一塁側）** | 設計仕様書 §3 の「打者の真横」を幾何学的に確定させた。打者の足は投手－捕手の軸に沿って並ぶため、この位置からだと**ステップと頭部の前後移動が画面の左右方向に現れる**（§9-1 が測定対象としているもの）。背中側（右打ちなら三塁側）でも幾何は同じだが、前膝とグリップが体で隠れやすいので正面側を推奨する |
| 2 | 距離の基準（スケール） | **胴体長（肩中点－股関節中点）を基準にする。投球版の「足首〜鼻」は使わない** | 打者は構えからフォロースルーまで**一度も直立しない**。投球版の「足首〜鼻の距離」は沈み込みで10%以上縮むため、打撃では使えない。胴体長は沈み込んでも変わらず、側面から見て短縮も小さい（§2-4） |
| 3 | 身長比への換算 | **`k_torso`（胴体長÷身長）を選手ごとに較正。既定 0.288** | 人体比（肩峰高 ≈ 身長の0.818、大転子高 ≈ 0.530）から胴体長 ≈ 0.288×身長。較正すればその選手について誤差が消える |
| 4 | cm 表示 | **較正済みのときのみ表示。未較正なら正規化値のみ** | 設計仕様書 §7 の要求そのもの |
| 5 | 始動（Commit）の検出 | **4つの候補を同時に提示し、指導者が採用するものを選ぶ**（自動で1つに決めない） | 設計仕様書 §4 の要求。単一信号では安定しない |
| 6 | インパクトの検出 | **手動確定を必須とする。未確定のスイングは保存できない** | 設計仕様書 §4「Ver.1Aでは手動指定を基本とし、自動候補はあくまで参考位置」。保存を止めることで、未確認の値が集計に混ざるのを防ぐ |
| 7 | トップ（割れ）の骨盤側の起点 | **側面カメラでは「見かけの股関節幅の増加開始」で近似する** | 設計仕様書 §4 はタイムラグの起点を「骨盤の向きの変化開始」としているが、§9-3 は回転を Ver.1B（正面カメラ）送りにしており、そのままでは Ver.1A で算出できない。側面からでも**骨盤が開くと左右股関節の見かけの幅が広がる**ため、これを起点として使う（§5-5） |
| 8 | 支持基底内の身体中心位置 | **算出する（削除しない）。ただし「体重配分」とは呼ばない** | 側面からは両足首と股関節中点が取れるので、支持基底内の相対位置は明確に定義できる。設計仕様書 §8 の改称方針に従う |
| 9 | CV（変動係数） | **正の量にのみ出す。符号つきで0をまたぐ指標には出さない** | 頭部移動量や膝伸展量は平均が0付近になりうるため、CV が発散して無意味になる |
| 10 | 測定信頼度の4段階 | **Good ≥0.80 / Fair 0.60–0.80 / Poor 0.40–0.60 / Invalid <0.40**。イベント未確定に依存する指標は最大 Fair に落とす | 設計仕様書 §10-1。イベントがずれていれば数値も同じだけずれるため、確定状態を信頼度に反映させる |
| 11 | ファイル構成 | **`web/batting-analyzer.html` を新規作成。投球版とは別ファイル** | 設計仕様書 §20 の「それぞれ独立して完成させ、共通エンジン化はその後」に従う |
| 12 | 保存先 | **IndexedDB（DB名 `battinglab`）。JSON 入出力を必須機能とする** | 投球版と同じ。iOS Safari はしばらく開かないと保存データを消すため、書き出しは消失防止の必須運用 |

---

## 2. 共通の前提

### 2-1. 座標系

MediaPipe Pose の正規化座標（`x`:0〜1 左→右、`y`:0〜1 上→下）。**y は下向きが正**。
幅と高さで別々に正規化されているため、全ての幾何計算は**等方座標**で行う。

```
AR = 動画の幅(px) / 動画の高さ(px)
X  = x * AR
Y  = y
```

### 2-2. 平滑化

各ランドマークの `x, y` に移動平均をかけてから角度・距離・速度を算出する。

```
n = round(0.050 * fps)
W = max(3, n が偶数なら n+1)        → 60fps:3  120fps:7  240fps:13
速度 v[i] = ( p[i+1] − p[i−1] ) / ( t[i+1] − t[i−1] )     （中心差分）
```

投球版と同じ。スイングは投球より速いので、fps を上げるほど窓が実時間で狭くなり有利になる。

### 2-3. 打席と前後の決定

- **打席**：選手プロフィールで指定（右打ち／左打ち）。自動判定はしない。
- **踏み込み足（前足）** ＝ 打席の反対側（右打ちなら左足）。**軸足**はその逆。
- **鏡像動画**のときは、解剖学的な側からランドマークの側へ変換する際に左右を入れ替える。

### 2-4. 投手方向 `dir`

踏み込み足は投手方向へ出るので、その移動向きで決める。

```
dir = sign( X_前足首(FS) − X_前足首(構え) )
```

`|X_前足首(FS) − X_前足首(構え)| / H_norm < 0.02` のときは判定不能とし、ユーザーに手動選択させる。

### 2-5. スケール（胴体長基準）

```
T_frame(t) = | 肩中点(t) − 股関節中点(t) |          （等方座標での距離）
T_norm     = T_frame の 90パーセンタイル（解析範囲全体）
H_norm     = T_norm / k_torso
値[身長比] = 値[等方単位] / H_norm
値[cm]     = 値[身長比] × 身長_cm                  （較正済みのときのみ表示）
```

⚠️ **投球版の「足首〜鼻 ÷ k_nose」は打撃では使わない。**
打者は構えからフォロースルーまで一度も直立せず、沈み込みで足首〜鼻の距離が大きく縮むため、
そのままでは距離指標がすべて過大になる。胴体長は沈み込みでは変わらない。

90パーセンタイルを採るのは、体幹が画面と平行に近いフレームほど短縮が小さく胴体長が大きく出るため。
最大値ではなくパーセンタイルにするのは、検出の外れ値1枚に引きずられないため。

**`k_torso` の決定（選手ごとに1回）**

| モード | 手順 | 精度 |
|---|---|---|
| **較正あり（推奨）** | ①画面内の既知長（メジャー等）を2点ドラッグして実長を入力 → `cm_per_unit` ②`k_torso = T_norm × cm_per_unit / 身長_cm` を算出して選手プロフィールに保存 | その選手について誤差なし |
| **未較正** | `k_torso = 0.288`（人体比の近似）。「未較正」ラベルを表示し **cm 表示は行わない** | 数%の誤差 |

較正時は姿勢を問わない（胴体長は姿勢に依らないため）。ただし**既知長は選手と同じ奥行きに置くこと**。

`k_torso` の妥当範囲は **0.24〜0.34**。外れた場合は入力ミスとみなして拒否する。

### 2-6. ランドマーク信頼度と測定信頼度

MediaPipe の `visibility` をイベントフレームごとに全33点保存する。

```
指標の生信頼度 = min( 依存ランドマークの visibility )
```

| 生信頼度 | ラベル | 扱い |
|---|---|---|
| ≥ 0.80 | **Good** | 通常表示 |
| 0.60 〜 0.80 | **Fair** | 通常表示（ラベル付き） |
| 0.40 〜 0.60 | **Poor** | グレー表示。集計には含めるが注記する |
| < 0.40 | **Invalid** | グレー表示。**平均・SD・比較から除外** |

さらに、**依存イベントが未確定の指標は、生信頼度に関わらず最大 Fair に落とす**。
イベントがずれていれば数値も同じだけずれるため。

⚠️ `visibility` は「画面内に見えていると推定される度合い」であって座標の正確さの保証ではない。
明らかに怪しい値を落とすフィルタとしてのみ使う。

### 2-7. フレームレート

スイングは始動からインパクトまで **0.15〜0.2秒程度**。タイミング指標の分解能は `1000/fps` ms。

| fps | 分解能 | 始動→インパクト(0.175s)が何コマか |
|---|---|---|
| 30 | 33.3 ms | 5〜6コマ |
| 60 | 16.7 ms | 10〜12コマ |
| 120 | 8.3 ms | 21〜24コマ |
| 240 | 4.2 ms | 42〜48コマ |

**iPhone16 のスローモーション（1080p / 120fps または 240fps）を推奨**する。
`fps < 60` のときはタイミング系のアラート・比較を行わず、「分解能不足のため判定していません」と表示する。

**スロー撮影の時間軸補正**：書き出し時にスローが焼き込まれている場合、
`slow_factor = max(1, capture_fps / 実測fps)`、`実時間 = 動画時間 / slow_factor`。
時間に関わる指標はすべて実時間で算出する。角度・距離・比率は影響を受けない。

---

## 3. ランドマークと派生点

| 名称 | index | 名称 | index |
|---|---|---|---|
| 鼻 | 0 | 左股関節 / 右股関節 | 23 / 24 |
| 左肩 / 右肩 | 11 / 12 | 左膝 / 右膝 | 25 / 26 |
| 左肘 / 右肘 | 13 / 14 | 左足首 / 右足首 | 27 / 28 |
| 左手首 / 右手首 | 15 / 16 | 左つま先 / 右つま先 | 31 / 32 |

```
肩中点   = (左肩 + 右肩) / 2
股関節中点 = (左股関節 + 右股関節) / 2
グリップ   = (左手首 + 右手首) / 2        ← バットは追跡しない（§6）
胴体長 T   = | 肩中点 − 股関節中点 |
```

---

## 4. イベント（5点）

自動検出は**候補の提示**。確定は手動。候補の前後 ±5 コマをサムネイルで提示し、1枚選んで確定する。

| ID | イベント | 検出方法 | 確定 |
|---|---|---|---|
| `setup` | 構え | トリミング開始フレーム | 任意 |
| `commit` | 始動 | §4-1（**4種類の候補を並べて提示**） | 必須 |
| `fs` | 踏み込み足接地 | §4-2 | 必須 |
| `impact` | インパクト | §4-3（**手動確定が必須**） | **必須** |
| `finish` | フォロースルー | `impact` 以降、グリップ速度が最大の25%を下回る最初 | 任意 |

`commit` `fs` `impact` のいずれかが未確定のスイングは**保存できない**。

### 4-1. 始動（Commit）の候補 — 4種類を並べて提示

設計仕様書 §4 の要求どおり、単一信号に頼らず4つを算出して**すべて画面に並べる**。
指導者がどれを採用するか選ぶ（自動でひとつに決めない）。

いずれも「その量が動き出した最初のフレーム」＝ 探索区間の最大値の 20% を 2フレーム連続で超えた最初。

| 候補 | 量 |
|---|---|
| ① グリップ | グリップ（両手首中点）の速さ [身長/秒] |
| ② 骨盤の開き | 見かけの股関節幅 `\|X_左股関節 − X_右股関節\|` の増加速度 |
| ③ 前足の移動 | 前足首の速さ [身長/秒] |
| ④ 肩の開き | 見かけの肩幅 `\|X_左肩 − X_右肩\|` の増加速度 |

探索区間は `setup` 〜 `impact`。既定の採用は **① グリップ**（最も安定するため）。

### 4-2. 踏み込み足接地（FS）の候補

前足の**足首**と**つま先**について、平滑化後の速さ `v`（身長/秒）が

```
条件1: v_足首   < 0.15 [身長/秒] が 2フレーム連続
条件2: v_つま先 < 0.20 [身長/秒] が 2フレーム連続
条件3: Y_足首 ≥ 0.95 × （それまでの Y_足首 の最大値）
```

を最初に満たすフレーム。満たさない場合は条件1のみ →「Y_足首 が最大のフレーム」の順にフォールバック。
探索区間は `commit` 〜 `impact`。閾値は設定から変更可能とする。

**遅れの補正（実装時に追加）**

速度が閾値を下回るのを待つ判定は、**平滑化（窓の半分）と中心差分（1コマ）のぶんだけ必ず接地より後ろにずれる**。
合成データでの実測では 30fps で **2コマ（67ms）遅れた**。
そこで、上で求めたフレームから、**前足首が「すでに最終位置に来ていた」最初のフレームまで遡る**。

```
EPS = 0.01 [身長比]                      （170cm なら約 1.7cm）
j = fs
while j > commit かつ |前足首(j−1) − 前足首(fs)| / H_norm ≤ EPS:  j = j − 1
fs = j
```

補正後の実測は 30fps で 1コマ。fps を上げるほど1コマあたりの移動量が小さくなるので、
遡れるフレーム数が増え、時間の遅れはさらに小さくなる。
**それでも候補は候補**であり、サムネイルでの手動確認は必須のままとする。

### 4-3. インパクトの候補（参考位置）

```
候補 = commit 以降で グリップの速さが最大となるフレーム
```

⚠️ **これは参考位置にすぎない。**
グリップ速度の最大とボール接触は一致しない（手の減速がヘッド加速に寄与するため、
グリップ速度のピークはインパクトより手前に出る傾向がある）。
**Ver.1A ではインパクトの手動確定を必須**とし、確定するまで
インパクトに依存する指標を「未確定」として扱う。

---

## 5. 測定項目

各項目の「依存」は §2-6 の信頼度算出に使うランドマーク。

### 5-1. ステップ幅

| | |
|---|---|
| 定義 | FS 時点の、前足首と軸足首の**投手方向成分の距離** |
| 計算 | `step_norm = abs( X_前足首(fs) − X_軸足首(fs) )` / `step_ratio = step_norm / H_norm × 100` |
| 単位 | `step_ratio` [%]（身長比）、較正済みなら `step_cm` |
| 保存 | `step_ratio`, `step_cm`, `conf_step` |
| 依存 | 27, 28 |

### 5-2. 頭部移動量（前後・上下）

| | |
|---|---|
| 定義 | 構え時の頭部位置を原点とした移動量。前後は投手方向を正、上下は上を正 |
| 計算 | `head_fwd(t) = dir*( X_鼻(t) − X_鼻(setup) ) / H_norm`<br>`head_vert(t) = ( Y_鼻(setup) − Y_鼻(t) ) / H_norm` |
| 保存 | `head_fwd_impact`, `head_vert_impact`（インパクト時）<br>`head_fwd_max`, `head_vert_drop`（構え〜インパクトの最大前方移動 / 最大沈み込み） |
| 単位 | 身長比（符号つき） |
| 依存 | 0（鼻）※ `setup` と当該フレームの両方 |
| 注意 | 側面カメラのため**打者から見た左右方向の頭の動きは測れない**。前後・上下のみ |

### 5-3. 前足の膝角度と伸展量（コア指標）

| | |
|---|---|
| 定義 | 前脚の 股関節–膝–足首 のなす角。**180° = 完全伸展** |
| 計算 | `knee_fs`（FS時）、`knee_impact`（インパクト時）、`knee_ext = knee_impact − knee_fs` |
| 単位 | 度 |
| 保存 | `knee_fs`, `knee_impact`, `knee_ext`, `conf_knee` |
| 依存 | 23/24, 25/26, 27/28（前脚側） |
| 判定 | **一律の目安角度は設けない。** 判定は本人平均との差のみ。<br>ただし `knee_ext ≤ 0`（曲がり続けている）は方向性が明確なため、本人比によらず注意を出す |

### 5-4. 軸足の膝角度（副次指標）

`back_knee_fs`, `back_knee_impact`。計算は 5-3 と同じで軸足側。

### 5-5. トップ（割れ）のタイムラグ

| | |
|---|---|
| 定義 | 骨盤が開き始めてから、グリップが投手方向へ動き出すまでの時間差 |
| 計算 | `t_pelvis_open` = 見かけの股関節幅 `\|X_左股関節 − X_右股関節\|` の**増加速度**が、その最大の20%を2フレーム連続で超えた最初<br>`t_grip_fwd` = グリップの `dir` 方向速度が正で、その最大の20%を2フレーム連続で超えた最初<br>`top_lag_ms = ( t_grip_fwd − t_pelvis_open ) / slow_factor × 1000` |
| 単位 | ms（正 = 骨盤が先に開いている＝割れができている向き） |
| 保存 | `top_lag_ms`, `t_pelvis_open`, `t_grip_fwd`, `conf_top` |
| 依存 | 23, 24, 15, 16 |
| 注意 | ⚠️ **3次元の捻転角度ではない。** 見かけの股関節幅から骨盤の開き始めを推定した2D近似であり、角度そのものは測っていない。負値（グリップが先）になることもあり、その場合はそのまま表示する |

### 5-6. グリップ速度（手首移動速度）

| | |
|---|---|
| 定義 | 両手首中点の移動速度。**バットヘッド速度ではない** |
| 計算 | `grip_speed(t) = \|d(グリップ)/dt\| / H_norm`（中心差分・実時間） |
| 単位 | 身長/秒 |
| 保存 | `grip_speed_max`（commit〜impact の最大）、`grip_speed_impact`（インパクト時）、`conf_grip` |
| 依存 | 15, 16 |
| 注意 | ⚠️ **UI・CSV とも「グリップ速度」と表記し、「バット速度」とは呼ばない。**<br>手首からヘッドまでのレバーがあるため、手首速度が低くてもヘッド速度が高いことがある |

⚠️ **平滑化により最大値は系統的に低めに出る。** スイングの加速は 50ms 程度で、平滑化窓（§2-2）と
同じ時間スケールにある。合成データでの実測では **最大値が 5% 低下**した（アプリの値は、同じ平滑化を
かけた独立実装と 0.00% 一致）。この低下は同じ設定なら毎回同じだけ起きるため本人比較には影響しないが、
**絶対値を他の計測機器や文献値と比べてはならない**。

### 5-7. 支持基底内の身体中心位置（近似）

| | |
|---|---|
| 定義 | 両足首を結ぶ区間の中で、股関節中点がどのあたりにあるか |
| 計算 | `base_pos(t) = ( X_股関節中点(t) − X_軸足首(t) ) / ( X_前足首(t) − X_軸足首(t) )` |
| 単位 | 比（0 = 軸足の真上、1 = 前足の真上）。範囲外もそのまま表示 |
| 保存 | `base_pos_fs`, `base_pos_impact` |
| 依存 | 23, 24, 27, 28 |
| 注意 | ⚠️ **これは体重配分ではない。**「体重配分 6:4」のような表現は使わない（設計仕様書 §8） |

### 5-8. タイミング

分解能は `1000/fps/slow_factor` ms。表示はその単位に丸める。

| 内部名 | 定義 | UI表示 |
|---|---|---|
| `Commit_to_FS_ms` | `t_fs − t_commit` | 始動→接地 |
| `FS_to_Impact_ms` | `t_impact − t_fs` | 接地→インパクト |
| `Commit_to_Impact_ms` | `t_impact − t_commit` | 始動→インパクト |
| `top_lag_ms` | §5-5 | トップのタイムラグ |

### 5-9. 結果（任意入力）

`exit_velocity_kmh`（打球初速）、`launch_angle_deg`（打ち出し角）、`spray_dir`（打球方向：左/中/右）。
Ver.1A では**入力欄のみ**。フォーム指標との相関分析は Ver.2（設計仕様書 §9-6）。

---

## 6. 再現性と比較

### 6-1. 統計量（設計仕様書 §9-8）

各指標について次を保存する。`Invalid` の値と、必須イベントが未確定のスイングは除外する。

| 名称 | 定義 |
|---|---|
| `mean` | 平均 |
| `median` | 中央値（外れ値に強い） |
| `sd` | 標準偏差（不偏、n ≥ 3 のときのみ） |
| `cv` | 変動係数 `sd / \|mean\| × 100` [%]。**正の量にのみ算出**（§1 決定9） |
| `n` | 集計に使った数（常に併記） |
| `mean_recent10` | 直近10スイングの平均 |
| `delta` / `delta_recent` | 今回値 − mean / mean_recent10 |

CV を出す指標：ステップ幅、グリップ速度、タイミング各種、**膝角度（前膝・軸足、FS時・インパクト時）**。
CV を出さない指標：頭部移動量、膝伸展量、トップのタイムラグ、支持基底内位置（0をまたぐため）。

※ 軸足の膝角度は当初の一覧に無かったが、前膝の角度とまったく同じ性質の量（常に正で 0 に近づかない）
なので、片方だけ CV を出さない理由がない。実装では両方に出している。

### 6-2. ベースライン成熟度（設計仕様書 §10-2）

| n | 扱い |
|---|---|
| n < 5 | **比較しない**。「データ蓄積中（あと◯本）」と表示 |
| 5 ≤ n < 10 | 参考値として比較。「参考」ラベルを付ける |
| n ≥ 10 | 通常比較 |

### 6-3. 動作変化（設計仕様書 §9-7）

セッション内で `swing_no` 昇順に10本ずつブロック化し、ブロック平均を並べる。
画面の見出しは **「後半スイングにおける動作変化」**とし、測定レイヤーでは「疲労」という語を使わない。
「疲労の可能性」は Interpretation 段階でのみ用いる。

---

## 7. 保存データスキーマ

保存先は **IndexedDB（DB名 `battinglab`）**。加えてセッション単位の JSON エクスポート／インポートを必須とする。

```
Swing {
  id, session_id, batter_id, swing_no, timestamp
  video_ref, fps, video_ar
  calib: { k_torso, calibrated, T_norm, H_norm, cm_per_unit|null,
           dir, dir_manual, capture_fps|null, slow_factor }
  events: { setup, commit, fs, impact, finish }      // 各 {frame, t_sec, confirmed}
  commit_source: "grip"|"pelvis"|"front_foot"|"shoulder"   // どの候補を採用したか
  metrics: { step_ratio, step_cm,
             head_fwd_impact, head_vert_impact, head_fwd_max, head_vert_drop,
             knee_fs, knee_impact, knee_ext, back_knee_fs, back_knee_impact,
             top_lag_ms, t_pelvis_open, t_grip_fwd,
             grip_speed_max, grip_speed_impact,
             base_pos_fs, base_pos_impact,
             Commit_to_FS_ms, FS_to_Impact_ms, Commit_to_Impact_ms,
             exit_velocity_kmh, launch_angle_deg, spray_dir }
  landmarks: { setup:[33×{x,y,vis}], commit:[...], fs:[...], impact:[...], finish:[...] }
  quality: { conf:{...}, label:{...}, checklist:{...}, fps_warning }
  note
}

Batter  { id, name, height_cm, bats:"R"|"L", k_torso, calibrated }
Session { id, batter_id, date, place, started_at, ended_at, swing_ids[] }
```

イベントフレームのランドマークを丸ごと保存するため、較正値や定義を変えても再計算できる。

### 7-1. 撮影チェックリスト（設計仕様書 §13）

セッション開始時に確認する。

```
□ 全身（頭〜足先、バットの先も可能なら）が画角に入っている
□ 極端に暗くない／逆光でない
□ カメラが水平（傾いていない）
□ 投手－捕手の線に対して直角の位置から撮っている
```

---

## 8. CSV エクスポート（設計仕様書 §14・Ver.1A 必須要件）

**1スイング1行**。UTF-8（BOM付き）。欠測は空欄。数値は小数第3位まで。
**列名は内部名称に統一**（多義的な一般用語を使わない）。

```
date, session_id, swing_no, batter_name, height_cm, bats, fps, capture_fps, slow_factor, calibrated,
Commit_to_FS_ms, FS_to_Impact_ms, Commit_to_Impact_ms, top_lag_ms,
step_ratio, step_cm,
head_fwd_impact, head_vert_impact, head_fwd_max, head_vert_drop,
knee_fs, knee_impact, knee_ext, back_knee_fs, back_knee_impact,
grip_speed_max, grip_speed_impact,
base_pos_fs, base_pos_impact,
commit_source,
exit_velocity_kmh, launch_angle_deg, spray_dir,
conf_step, conf_head, conf_knee, conf_grip, conf_top,
label_step, label_head, label_knee, label_grip, label_top,
event_commit_confirmed, event_fs_confirmed, event_impact_confirmed,
fps_warning, note
```

外部送信は行わない（設計仕様書 §16）。

---

## 9. アラート（設計仕様書 §12）

4階層（Measurement → Comparison → Alert → Interpretation）を適用し、画面上も分離する。
比較対象は `delta_recent`。**§6-2 の成熟度が満たされないときはアラートを出さない。**

| 指標 | Level 1 | Level 2 |
|---|---|---|
| グリップ速度（最大） | −8% | −15% |
| ステップ幅（身長比） | ±3 pt | ±6 pt |
| 前膝の伸展量 | −5° | −10°（`knee_ext ≤ 0` は無条件で Level 1） |
| 頭部の前方移動（インパクト時） | ±0.03 | ±0.06 |
| 頭部の沈み込み | ±0.03 | ±0.06 |
| Commit_to_Impact_ms | ±15 ms | ±30 ms |
| トップのタイムラグ | ±20 ms | ±40 ms |

**Level 3（複合）**：Level 2 が同時に2項目以上、または
`グリップ速度 Level1以上の低下` かつ `頭部移動量の増加` かつ `前膝伸展量の低下` の3点同時。

⚠️ **これらは外部から与えた初期値であり、その選手に適切である保証はない。**
全項目を設定から変更可能にし、10本程度たまった時点で本人の SD を見て再設定する運用を前提とする。
`fps < 60` のときタイミング系のアラートは出さない（§2-7）。

---

## 10. ドリル候補 — **実装しない（方針変更）**

前版では `Drill` / `DrillRule` を持ち、ユーザーがドリルを登録して条件に紐付ける
「仕組みだけ」を提供する方針だった。**これを取りやめ、機能ごと削除する。**

理由：

- 本ソフトの役目は**測って、本人の過去と比べて、違いを示すところまで**である。
  そこから先の「では何をするか」は指導者の領域で、システムが場所を用意するだけでも
  「ここに書かれた練習をすればよい」という受け取られ方をしうる。
- 空の入れ物を置いておくと、埋めるべきものがあるように見える。
  作成者は打撃指導の専門家ではないため、埋め方の良し悪しも示せない。

これに伴い、表示階層は5段から **4段（測定 → 比較 → 判定 → 解釈）** とする。


### 保存済みレコードどうしの比較（実装時に追加）

イベントのコマの全ランドマークを保存してあるので、**動画がもう手元になくても**2件を重ねて比べられる。

- 2件とも**それぞれの `H_norm` で割ってから**重ねる。撮影距離やカメラ位置が違っても比べられる。
- 重ねる基準点は**両足首の中点（地面に近い）／股関節中点／肩中点**から選ぶ。
  足首基準は全身の位置関係、股関節基準は手足の使い方が見やすい。
- **縦横は同じ縮尺**で描く（単位が同じ身長比なので、軸ごとに変えると形が歪む）。
  2体を囲む枠が画面いっぱいになるように収める。
- ⚠️ **カメラの角度が違うと形そのものが変わる。**これは正規化では補正できないので、
  同じ位置から撮った動画どうしで比べる旨を画面に明記する。
- 指標は A / B / **B − A** の3列で並べる。どちらが良いかは判定しない。

---

---

## 11. 画面構成（設計仕様書 §15）

| 画面 | 内容 |
|---|---|
| 概要 | このセッションの本数・平均グリップ速度・平均ステップ幅・打球データの入力状況（実装時に追加） |
| 打者・撮影 | 選手登録（身長・打席）、較正、撮影チェックリスト |
| 解析 | セッション、トリミング、事前解析、イベント5点の候補と確定 |
| 指標 | §5 の各値＋測定信頼度ラベル、打球データ入力、スイングの記録 |
| タイミング | 始動→FS→インパクトのタイムライン、トップのタイムラグ |
| 再現性 | 平均・中央値・SD・CV・n、成熟度表示 |
| 比較 | 保存済みの2スイングを選び、イベントのコマの骨格を重ねて表示＋指標の差（実装時に追加） |
| 変化 | 10本ブロックごとの推移（「後半スイングにおける動作変化」） |
| 品質 | ランドマーク信頼度・測定信頼度・チェックリスト・fps 警告 |

---

## 12. 実装順（設計仕様書 §19）

```
Phase 1  動画読込・骨格表示・コマ送り・トリミング・平滑化・較正・打者/セッション管理
Phase 2  イベント5点の候補提示（始動は4候補）＋サムネイル確定
Phase 3  §5 の指標算出＋測定信頼度
Phase 4  タイミングとトップのタイムラグの可視化
Phase 5  複数スイング比較（平均・中央値・SD・CV・n、成熟度判定）
Phase 5.5 CSV／JSON 入出力
Phase 6  動作変化・アラート
```

---

## 13. Ver.1A で実装しないこと

カメラ自動同期／3D解析／真の Hip-Shoulder Separation／色マーカーによるバット追跡／
Attack Angle／体幹側屈・後ろ肘の絞り・頭部傾斜角・キャスティング検知／
球種別・コース別解析／ベストスイング分析／打球データとの相関分析／
LLM による自然文講評／閾値による自動エラー判定。

---

## 付録. 用語の扱い（誤解を防ぐための取り決め）

| 使わない語 | 代わりに使う語 | 理由 |
|---|---|---|
| バット速度 | **グリップ速度（手首移動速度）** | バットは追跡していない |
| Hip-Shoulder Separation | （Ver.1A では算出しない） | 3次元の回旋角度差を指す用語のため |
| 体重配分 | **支持基底内の身体中心位置（近似）** | 体重は測っていない |
| 疲労解析 | **後半スイングにおける動作変化** | 変化の要因は疲労とは限らない |
| Swing Window | **Commit_to_Impact_ms** | 多義的な一般用語のため |

# 投球動作解析システム Ver.1 測定項目仕様書 Ver.1.1
### 設計仕様書 Ver.1.0 §18「次工程」に対応する詳細仕様（未決事項をすべて確定した版）

本書は設計仕様書 Ver.1.0 §9（Ver.1 コア指標 A〜F）について、
**どのランドマークからどう計算し、何を単位とし、何を保存するか**を実装可能な粒度まで確定した文書。

Ver.1.0（本書の前版）で「要判断」として保留していた8件、および暫定扱いだった項目を
すべて決定した。**本書に未決事項はない。この内容で実装に着手できる。**

決定には理由を併記した（§1）。方針が合わない箇所があれば、その項目だけを指摘してもらえば差し替える。

---

## 1. 決定事項一覧

### 1-1. 前版で「要判断」としていた8件

| # | 論点 | **決定** | 理由 |
|---|---|---|---|
| 1 | 身長入力の要否 | **選手プロフィールとして必須。未入力では解析を開始できない** | 入力は1選手1回で済む。cm 表示は指導現場で伝わりやすく、成長追跡（§10 経時比較）にも要る。「任意」にすると cm 列が空の投球と埋まった投球が混在し、CSV の後工程が煩雑になる |
| 2 | 投球板マーク | **採用しない。原点はセット時の軸足つま先 X に一本化** | 投球板は屋内ブルペンや画角の都合で写らないことがあり、必須にすると解析不能な動画が生じる。任意にすると原点の異なる値が混在し集計できない。**その代わり指標名から「エクステンション」の語を外し「リリース前後位置（軸足基準）」とする**（測っていないものを測ったように呼ばない、設計仕様書 §3 の方針） |
| 3 | Arm Peak の探索窓 | **`fp − 0.15秒` 〜 `rel`** | 正常な投球では Arm Peak は FP 直後に現れる。`lift` まで遡ると、脚を上げる途中の肘の高さでスコアが最大化され誤検出する。0.15秒 の余裕で早期コッキング（FP 前にピーク）も捉えられ、その場合 D-1 が負値になり事実として表示される |
| 4 | `knee_rel` の測定時点 | **リリースフレーム** | 定義が素直で、C-2 の意味（接地から放すまでに前脚がどう働いたか）と一致する。「FP＋固定時間」は投球ごとのテンポ差を無視してしまう |
| 5 | セッションの単位 | **ブルペン1本単位（手動で開始・終了）。日付は属性として保持** | 疲労解析の10球ブロックは連続した投球でなければ意味がない。日付単位だと1日2回の登板が混ざる。操作は開始・終了の2回のみ |
| 6 | データ保存先 | **IndexedDB。加えてセッション単位の JSON エクスポート／インポートを必須機能とする** | §6-1 の試算で1球あたり6〜8KB、100球で0.6〜0.8MB。localStorage（約5MB）では数百球で頭打ち。IndexedDB ならブラウザ内で完結し、外部送信しない設計（設計仕様書 §15）を維持できる。<br>⚠️ **iOS Safari は、一定期間サイトを開かないと保存データ（localStorage・IndexedDB とも）を自動削除することがある。**「ホーム画面に追加」して定期的に使えば削除されにくくなるが、保証はない。**JSON エクスポートは端末移行用ではなく、データ消失を防ぐための必須運用**と位置づけ、UI から定期的な書き出しを促す |
| 7 | 打撃版との関係 | **新規ファイル `web/pitching-analyzer.html` を Ver.1 専用として作る。現行 `formanalyzer.html` は当面そのまま残し、Ver.1 完成をもって投球モードを削除して打撃専用にする** | 本仕様は投球単位のデータストア・球速入力・アラート・ドリルなど、打撃版と共有できないものが中核。同居させると打撃側に不要な UI が増える。<br>⚠️ 共通部分（動画読込・骨格検出・コマ送り等）は一時的に二重管理になる。**単一 HTML ファイルにしているのは `file://` で直接開けるようにするためで、共通モジュールを別 `.js` に切り出すと `file://` からの読み込みがブラウザに拒否される**。Ver.1 完成時に投球モードを削除して二重管理を解消する |
| 8 | 既存の「目安値」表示 | **Ver.1 では判定にも表示にも使わない。付録 A の読み物としてのみ残す** | 設計仕様書 §1 原則1・§8 と正面から矛盾するため。現行アプリからの明確な仕様変更 |

### 1-2. 前版で「暫定」としていた項目

| 論点 | **決定** | 理由 |
|---|---|---|
| 身長ピクセル長の人体比係数 `k_nose` | **選手ごとに1回の「較正」で実測する（§2-4）。未較正時は既定 0.89 を使い「未較正」ラベルを付ける** | 較正すれば人体比の近似誤差が消える。較正は1選手1回で済む |
| アラート閾値 | **§8-2 の固定値で確定。Ver.1 では本人 SD による自動再設定は行わない** | SD ベースの閾値は球数が少ないと不安定（設計仕様書 §16 が Z-score を Ver.1 から外している理由と同じ）。設定画面から手動変更は可能にする。SD ベースへの移行は Ver.1.5（設計仕様書 §16-1） |
| ドリル候補の初期データ | ~~既定は空~~ → **機能ごと削除（§9）** | 作成者は投球指導の専門家ではない。空の入れ物でも「埋めるべきもの」に見え、指導内容の提示は本ソフトの役目ではないため |
| リリース高さの足首→地面補正（+0.04） | **適用しない。指標名を「リリース高さ（足首基準）」とする** | 本人比では補正の有無は打ち消し合う。隠れた定数を持たせるより、基準を名称に書くほうが誤解が少ない |

### 1-3. 前版に記述がなく、本版で新たに決めたもの

| 論点 | **決定** |
|---|---|
| `mean_recent10` の範囲 | セッションを跨いで直近10球。算出に使ったセッション数を併記する |
| 疲労ブロック | セッション内で完結。`pitch_no` 昇順に1〜10, 11〜20… と機械的に区切る |
| SD の表示下限 | n ≥ 3。それ未満は「—」 |
| 欠測値 | 数値は空（`0` を入れない）。平均・SD の集計から除外し、集計に使った n を必ず併記 |
| 投球方向 `dir` の判定不能時 | 骨盤中心の移動量が身長比 2% 未満のとき、ユーザーに手動選択させる |
| 同一動画に複数球ある場合 | トリミングで1球ずつ切り出し、1球＝1レコードとして登録する |

---

## 2. 共通の前提

### 2-1. 座標系

MediaPipe Pose の正規化座標（`x`:0〜1 が画像左→右、`y`:0〜1 が画像上→下）を入力とする。
**y は下向きが正**である点に注意（高さを扱う式で符号を間違えやすい）。

`x` と `y` は画像の幅・高さでそれぞれ独立に正規化されているため、そのままでは角度・距離が歪む。
全ての幾何計算は次の**等方座標**に変換してから行う。

```
AR = 動画の幅(px) / 動画の高さ(px)
X  = x * AR
Y  = y
```

以降、大文字の `X, Y` は等方座標を指す。

### 2-2. 平滑化

生座標を直接微分すると検出ノイズが増幅されるため、各ランドマークの `x, y` に**移動平均**をかけてから
角度・距離・速度を算出する。窓幅は時間基準で決め、fps が違う動画でも平滑化の強さを揃える。

```
n = round(0.050 * fps)
W = max(3, n が偶数なら n+1)          → 30fps:3  60fps:3  120fps:7  240fps:13
速度 v[i] = ( p[i+1] − p[i−1] ) / ( t[i+1] − t[i−1] )     （中心差分）
```

fps が低いと 3 フレームでも 50ms を超える（30fps では 100ms）。移動平均の最小窓が 3 のため避けられない。
実装は算出した窓幅を UI に表示し、実際に何 ms 分を平均したかが分かるようにする。

両端は窓が取れないため、利用可能なフレームだけで平均する（片側平均）。
イベント検出も表示も同一の平滑化系列を使う（検出用と表示用で別の系列を持たない）。

### 2-3. 投球方向 `dir` の決定

```
dir = sign( X_骨盤中心(fp) − X_骨盤中心(set) )
```

`dir = +1` なら画面右が捕手方向、`−1` なら画面左。以降「前方」はこの向きを指す。
`|X_骨盤中心(fp) − X_骨盤中心(set)| / H_norm < 0.02` のときは判定不能とし、ユーザーに手動選択させる。

### 2-4. スケールと較正

距離指標は**すべて身長比（無次元）を一次値**とし、cm 表示はその従属値とする。
カメラ距離が試技ごとに変わっても比較できるようにするため。

**身長ピクセル長**

```
H_frame(t) = ( Y_足首中点(t) − Y_鼻(t) ) / k_nose
H_norm     = H_frame の 90パーセンタイル（解析範囲全体）
値[身長比] = 値[等方単位] / H_norm
値[cm]     = 値[身長比] × 身長_cm
```

⚠️ **`set` フレーム1枚から求めてはならない。** トリミング開始が既に脚上げに入っていると、
足首中点が上がるぶん `H_norm` が 10% 近く小さく出て、距離指標すべてが過大になる
（実測: 直立フレーム 0.663 に対し脚上げ中 0.592）。
直立に近い姿勢ほど 足首〜鼻 の距離は大きくなるため、範囲全体の 90 パーセンタイルを採る。
単純な最大値ではなくパーセンタイルにするのは、検出の外れ値1枚に引きずられないため。

**`k_nose` の決定（選手ごとに1回）**

| モード | 手順 | 精度 |
|---|---|---|
| **較正あり（推奨）** | ①選手が直立静止しているフレームを選ぶ ②画面内の既知長（メジャー、投球板の横幅 60.96cm 等）を2点ドラッグして実長を入力 ③`cm_per_unit` が求まる ④`k_nose = ( Y_足首中点 − Y_鼻 ) / ( 身長_cm / cm_per_unit )` を算出して選手プロフィールに保存 | その選手について誤差なし |
| **未較正** | `k_nose = 0.89` を使用 | 数%の誤差。**「未較正」ラベルを表示** |

`k_nose = 0.89` は「足首〜鼻の鉛直距離が身長の約89%」という人体比の近似（鼻の高さ ≈ 身長の93%、
外果の高さ ≈ 身長の4%）。年少者は頭部が相対的に大きく `k_nose` はやや小さめ（0.88前後）になりうる。

⚠️ **未較正の値を選手間の比較や cm の絶対評価に使ってはならない。**
同一選手の中での比較（＝設計仕様書 §1 原則2）では誤差が打ち消し合うため、未較正でも支障はない。

較正は**カメラ設置が変わっても再実行は不要**（`k_nose` は選手の身体比であってカメラの性質ではない）。
ただし既知長の指定と同じ奥行き平面にいることが前提なので、較正時は選手を撮影位置に立たせて行う。

### 2-5. ランドマーク信頼度

MediaPipe が出力する `visibility` を、**イベントフレームごとに全33点分そのまま保存**する。
各測定項目には「依存ランドマーク」を定義する（§5 の各項目に明記）。

```
指標の信頼度 = min( 依存ランドマークの visibility )
```

| 信頼度 | 扱い |
|---|---|
| ≥ 0.70 | 通常表示 |
| 0.50 〜 0.70 | 「信頼度低め」ラベルを付けて表示。集計には含める |
| < 0.50 | グレー表示。**平均・SD・アラートの集計から除外**し、除外した旨と n を表示 |

⚠️ **`visibility` は「そのランドマークが画面内に見えていると推定される度合い」であり、座標の正確さの保証ではない。**
値が高くても誤検出はありうる。明らかに怪しい数値を落とすフィルタとしてのみ使う。この注記を UI に常時表示する。

### 2-5b. スロー撮影の時間軸補正（運用端末: iPhone）

⚠️ **iPhone のスローモーション動画は、書き出し方によっては「遅くした状態」で焼き込まれる。**
240fps で撮った動画がファイル上 30fps・8倍の長さになっている場合、
fps 実測値は 30 となり、**時間に関わる指標がすべて8倍ずれる**（テンポ、FP→リリース、速度）。
角度・位置・比率の指標は影響を受けない。

対策として `capture_fps`（撮影時のfps）を設定項目に持ち、次で補正する。

```
slow_factor = max(1, capture_fps / 実測fps)      capture_fps 未設定なら 1
実時間[s]   = 動画時間[s] / slow_factor
```

- 時間に関わる指標（§5-D タイミング、速度）はすべて実時間で算出・保存する。
- UI には実測fps と 1コマの実時間長（例：`fps 29.97 ×8 / 1コマ 4.2ms`）を常時表示し、
  撮影時の設定と食い違っていればユーザーが気づけるようにする。
- 保存レコードに `capture_fps` と `slow_factor` を持ち、CSV にも出力する。

**運用手順**：動画を読み込んだら fps 表示を確認する。撮影時の設定と違っていれば `capture_fps` を指定する。

### 2-6. フレームレートと時間分解能

タイミング指標（§5-D）の分解能は `1000 / fps` [ms]。

| fps | 分解能 | FP→リリース（成人でおよそ120〜160ms）が何コマか |
|---|---|---|
| 30 | 33.3 ms | 4〜5コマ |
| 60 | 16.7 ms | 7〜10コマ |
| 120 | 8.3 ms | 14〜19コマ |
| 240 | 4.2 ms | 29〜38コマ |

本人比で検出したい変化量は ±10〜20ms 程度であり、**30fps では量子化誤差（±33ms）が変化量より大きい。**
これが設計仕様書 §4 で 60fps 以上を推奨する具体的な根拠である。

**運用端末（iPhone）での推奨**：カメラの「スローモーション」（1080p/240fps 等）で撮影する。
ただし §2-5b の通り、書き出し後のファイルが実際に何 fps になっているかを必ず確認すること。
上表の「分解能」は**実時間換算**（＝動画上の 1/fps を `slow_factor` で割った値）で判断する。

実装要件：
- 実 fps を計測して常時表示する。
- タイミング指標の表示は分解能単位に丸める（30fps で「+12ms」と表示しない）。
- **fps < 50 のとき、タイミング系のアラートを出さず「分解能不足のため判定していません」と表示する。**

---

## 3. ランドマークと派生点

| 名称 | index | 名称 | index |
|---|---|---|---|
| 鼻 | 0 | 左股関節 / 右股関節 | 23 / 24 |
| 左肩 / 右肩 | 11 / 12 | 左膝 / 右膝 | 25 / 26 |
| 左肘 / 右肘 | 13 / 14 | 左足首 / 右足首 | 27 / 28 |
| 左手首 / 右手首 | 15 / 16 | 左つま先 / 右つま先 | 31 / 32 |

```
肩中点   = (左肩 + 右肩) / 2
骨盤中心 = (左股関節 + 右股関節) / 2      ← 「重心」とは呼ばない（設計仕様書 §5）
L_胴体   = |肩中点 − 骨盤中心|            （等方座標での距離）
```

**投球腕**：選手プロフィールで指定（右/左）。**踏み出し脚 = 投球腕の反対側**、軸足はその逆。
自動推定は行わない（プロフィールで確定しているため）。

---

## 4. イベント検出

自動検出は**候補生成**であり、確定は必ず手動（設計仕様書 §4）。
UI は候補フレームの前後 ±5 コマをサムネイル列で提示し、ユーザーが1枚選んで確定する。
確定済みかどうかを `confirmed` フラグで保存し、未確定の投球は集計から除外できるようにする。

### 4-0. 手動確定の操作要件（コマ単位の移動）

手動確定の精度は**コマ送りが正確であること**に完全に依存するため、次を要件とする。

**① コマ送りは実フレーム時刻（`mediaTime`）を基準にする。**
`currentTime += 1/fps` で送る実装は、推定 fps と実フレーム間隔のわずかな差が積み上がり、
**数コマに1回、2コマ飛ぶ**。リリースを1コマ単位で確定する用途では実害がある。
`requestVideoFrameCallback` が返す `mediaTime`（表示中フレームの正確な時刻）を基準に、
目標フレームの内側 1/4 の位置へシークする。

```
f    = 1/fps
base = 表示中フレームの mediaTime
移動先 = base + n*f + 0.25*f        （n コマ移動。前方・後方とも +0.25f）
```

`mediaTime` はフレームの**開始**時刻なので、n コマ先のフレームは `[base+n*f, base+(n+1)*f)` の区間。
その 1/4 内側に着地させれば、fps 推定に多少の誤差があっても必ず目的のフレームに入る。
**符号を掛けて `sign(n)*0.25f` としてはならない**（後方移動で2コマ戻る）。

連打・長押し時に読み戻しが間に合わない場合に備え、予測フレーム開始時刻を保持し、
読み戻しが届くたびに実測値へ補正する。

**② 0.001秒単位の微調整を用意する。**
`currentTime` を 0.001 の格子に丸めて増減する。

⚠️ **1ms は 1コマより細かい**（60fps で 16.7ms、240fps でも 4.2ms）ため、
0.001秒送りでは**表示フレームが変わらないことのほうが多い**。
これを隠すと「ボタンが効かない」と誤解されるため、UI に次を必ず表示する。

- 現在のコマ番号（`mediaTime` から算出した実測値）
- 1コマの長さ（例：`1コマ 33.4ms`）
- 直前の操作でフレームが変わらなかったときの「同じコマ」表示
- シーク位置がフレーム時刻とずれているときの「位置」表示

**③ 検証項目**：連続25回のコマ送りで Δframe が常に ±1 であること、
10コマ進んで10コマ戻ると同一フレームに戻ること、
長押し（90ms間隔）でもコマ落ち・重複がないこと。

| ID | イベント | 検出方法 | 探索窓 |
|---|---|---|---|
| `set` | セット/構え | トリミング開始フレーム | — |
| `lift` | 最大脚上げ | 踏み出し足首の `Y` が最小（＝画面上で最も高い）フレーム | `set` 〜 `fp` |
| `fp` | 前足接地 | §4-1 | `lift` 〜 解析範囲終端 |
| `arm` | Arm Peak (2D近似) | §4-2 | **`fp − 0.15秒` 〜 `rel`** |
| `rel` | リリース | §4-3 | `fp` 〜 解析範囲終端 |
| `fin` | フィニッシュ | `rel` 以降、投球側手首速度が最大値の25%を下回る最初のフレーム | `rel` 〜 終端 |

`lift` と `fp` は相互に依存するため、次の順で解く：
① 仮 `fp` ＝ 解析範囲後半で踏み出し足首 `Y` が最大のフレーム → ② `lift` を確定 → ③ §4-1 で `fp` を確定。

### 4-1. FP（前足接地）候補

踏み出し脚の**足首**と**つま先**について、平滑化後の速度 `v = |d(X,Y)/dt|`（単位：身長/秒）を求め、
`lift` 以降で次を最初に満たすフレームを第1候補とする。

```
条件1: v_足首   < 0.15 [身長/秒]  が 2フレーム連続
条件2: v_つま先 < 0.20 [身長/秒]  が 2フレーム連続
条件3: Y_足首 ≥ 0.95 × ( それまでの Y_足首 の最大値 )
```

3条件すべてを満たす最初のフレーム＝第1候補。条件1のみ満たすフレーム＝第2候補。
どれも満たさなければ「`lift` 以降で `Y_足首` が最大のフレーム」を候補とする。

閾値 `0.15 / 0.20 / 0.95` は設定画面から変更可能とする。

### 4-2. Arm Peak（2D近似）候補

⚠️ **これは真の肩最大外旋角度（MER）ではない。**
UI 上に「本システムは肩の最大外旋角度を測定していない」旨を**常時**表示する（設計仕様書 §4）。
画面上の表記は常に **`Arm Peak (2D近似)`** とし、`MER` の語は使わない。

投球側の肘について、次のスコアが最大となるフレームを候補とする。

```
後方度 = dir * ( X_肩 − X_肘 ) / L_胴体      ← 肘が肩より後方にあるほど大
高さ度 = ( Y_肩 − Y_肘 ) / L_胴体            ← 肘が肩より上にあるほど大
score  = 後方度 + 高さ度
探索窓 = fp − 0.15秒 〜 rel
```

`L_胴体` で割ることで、カメラ距離が変わってもスコアの意味が変わらない。

### 4-3. リリース候補（Ver.1 最重要イベント）

設計仕様書 §4 の記述「手首のY座標が最下点を過ぎた直後、速度が最大になるフレーム」は
y 軸の向きが曖昧なため、本書で次のように確定する。

```
窓   : fp 以降、かつ 投球側手首が肩より前方（ dir*(X_手首 − X_肩) > 0 ）である区間
候補 : その窓で 投球側手首速度 |d(X,Y)/dt| が最大となるフレーム
```

⚠️ **手のピーク速度とボールリリースは厳密には一致しない。**
前腕・手の減速がボール加速に寄与するため、ピーク速度はリリース直前に来る傾向がある。
Ver.1 ではこの差を補正せず、上の定義を「候補」とし、**手動確定を必須**とする。
UI では候補の前後 ±5 コマを並べ、ボールが手から離れた最初のコマを選ばせる。
§4-0 のコマ送り（1コマ単位＋0.001秒微調整）で前後を確認できるようにする。
同一の定義で一貫して測る限り、本人比（設計仕様書 §1 原則2）の比較には支障がない。

---

## 5. 測定項目

各項目の「依存」は §2-5 の信頼度算出に使うランドマーク。

### A. 結果

#### A-1. 球速

| | |
|---|---|
| 定義 | 外部計測器（スピードガン等）の値をユーザーが手入力 |
| 単位 | km/h（小数第1位） |
| 保存 | `speed_kmh`（未入力可＝空） |
| 依存 | なし |
| 注意 | 自動計測はしない。未入力の投球は球速に関する集計・アラートから除外する |

### B. 移動

#### B-1. ストライド長／身長比

| | |
|---|---|
| 定義 | FP 時点の、軸足首と踏み出し足首の**投球方向成分の距離** |
| 計算 | `stride_norm = abs( X_踏み出し足首(fp) − X_軸足首(fp) )`<br>`stride_ratio = stride_norm / H_norm × 100` |
| 単位 | `stride_ratio` [%]、`stride_cm = stride_ratio / 100 × 身長_cm` |
| 保存 | `stride_ratio`, `stride_cm`, `conf_stride` |
| 依存 | 27, 28 ＋ `set` の 0, 27, 28 |
| 注意 | 真のストライドは投球板から踏み出し足までの距離である。本値は**両足首間**であり、FP 時に軸足が板から離れていればその分小さくなる。同一選手・同一測定法での比較にのみ使う |

#### B-2. 骨盤中心の前方移動量・移動速度

| | |
|---|---|
| 定義 | `lift` から `fp` までの骨盤中心の投球方向への移動 |
| 計算 | `pelvis_disp = dir * ( X_骨盤中心(fp) − X_骨盤中心(lift) ) / H_norm`<br>`pelvis_vel  = pelvis_disp / ( t_fp − t_lift )` |
| 単位 | `pelvis_disp` [身長比]、`pelvis_vel` [身長/秒] |
| 保存 | `pelvis_disp`, `pelvis_vel`, `conf_pelvis` |
| 依存 | 23, 24 ＋ `set` の 0, 27, 28 |
| 注意 | 側面カメラのため**奥行き方向の移動は測れない**。カメラが真横から外れているほど過小評価になる。設計仕様書 §7-2 の通り「並進運動を評価する指標の一つ」であり、球速の主因ではない |

### C. ブレーキ（下半身→上半身の変換）

#### C-1. FP 時点の前膝角度

| | |
|---|---|
| 定義 | 踏み出し脚の 股関節–膝–足首 のなす角。**180° = 完全伸展** |
| 計算 | `knee_fp = angle( 股関節, 膝, 足首 )`（等方座標、2D） |
| 単位 | 度（整数） |
| 保存 | `knee_fp`, `conf_knee_fp` |
| 依存 | 23/24, 25/26, 27/28（踏み出し側） |

#### C-2. 前膝の伸展量（FP→リリース）

| | |
|---|---|
| 定義 | `knee_ext = knee_rel − knee_fp`（正 = 伸展、負 = 屈曲継続） |
| 計算 | `knee_rel` は**リリースフレーム**で C-1 と同じ式 |
| 単位 | 度（整数） |
| 保存 | `knee_rel`, `knee_ext` |
| 依存 | C-1 と同じ（`fp` と `rel` の両フレーム） |
| 判定 | **一律の目安角度は設けない**（設計仕様書 §8）。判定は本人平均との差のみ。<br>ただし `knee_ext ≤ 0`（曲がり続けている）は方向性が明確なため、本人比によらず Level 1 以上のアラートを出す |

#### C-3. FP 時点の肘角度

| | |
|---|---|
| 定義 | 投球腕の 肩–肘–手首 のなす角 |
| 計算 | `elbow_fp = angle( 肩, 肘, 手首 )` |
| 単位 | 度（整数） |
| 保存 | `elbow_fp`, `conf_elbow` |
| 依存 | 11/12, 13/14, 15/16（投球側） |

#### C-4. 体幹前傾角

| | |
|---|---|
| 定義 | 骨盤中心→肩中点のベクトルと鉛直上方のなす角。**投球方向へ倒れる側を正** |
| 計算 | `trunk = atan2( dir*(X_肩中点 − X_骨盤中心), (Y_骨盤中心 − Y_肩中点) ) × 180/π` |
| 単位 | 度（小数第1位） |
| 保存 | `trunk_fp`, `trunk_rel` |
| 依存 | 11, 12, 23, 24 |

### D. タイミング

いずれも分解能は `1000/fps`（§2-6）。表示はその単位に丸める。

| ID | 定義 | 単位 | 保存 |
|---|---|---|---|
| D-1 | `t_arm − t_fp` | ms | `t_fp_arm` |
| D-2 | `t_rel − t_fp` | ms | `t_fp_rel` |
| D-3 | `t_rel − t_arm` | ms | `t_arm_rel` |

D-1 が負値（Arm Peak が FP より前）の場合もそのまま保存し、UI に「FPより前」と注記する。
早期コッキングのサインでありうるが、**Ver.1 では解釈を与えず事実のみ表示**する。

### E. リリース

#### E-1. リリース高さ（足首基準）

| | |
|---|---|
| 定義 | FP 時の低いほうの足首から、投球側手首までの鉛直距離 |
| 計算 | `Y_基準 = max( Y_左足首(fp), Y_右足首(fp) )`（y は下向き正のため max が地面に近い）<br>`rel_height = ( Y_基準 − Y_手首(rel) ) / H_norm` |
| 単位 | 身長比（小数第3位）、cm 換算可 |
| 保存 | `rel_height`, `conf_release` |
| 依存 | 15/16（投球側）, 27, 28 ＋ `set` の 0, 27, 28 |
| 注意 | 地面ではなく**足首**が基準（足首は地面より身長の約4%上）。補正は掛けない。名称に基準を明記する |

#### E-2. リリース前後位置（軸足基準）

| | |
|---|---|
| 定義 | セット時の軸足つま先から、投球側手首までの投球方向の距離 |
| 計算 | `rel_ext = dir * ( X_手首(rel) − X_軸足つま先(set) ) / H_norm` |
| 単位 | 身長比（小数第3位）、cm 換算可 |
| 保存 | `rel_ext` |
| 依存 | 15/16（投球側）, 31/32（軸足）＋ `set` の 0, 27, 28 |
| 注意 | **本来のエクステンション（投球板からの距離）ではない。**「エクステンション」の語は UI でも使わない |

#### E-3. リリース位置の再現性

| | |
|---|---|
| 定義 | 複数球の `rel_height` と `rel_ext` のばらつき |
| 計算 | 各々の標準偏差（不偏、n−1）。加えて2次元散布図（横軸 `rel_ext`、縦軸 `rel_height`）を描画（設計仕様書 §8-1） |
| 単位 | 身長比 |
| 保存 | 集計時に算出（投球レコードには保存しない） |
| 注意 | n < 3 では SD を表示しない。Ver.2 で左右位置が加われば3軸に拡張 |

**散布図の描画要件（実装時に確定）**

- `rel_height` と `rel_ext` は**同じ単位（身長比）**なので、**縦横を同じ縮尺で描く**。
  軸ごとに引き伸ばすと散らばりの「形」が実際と変わってしまい、
  「前後にはブレるが高さは安定している」といった読み取りができなくなる。
- そのため描画領域は**正方形**にし、枠線を描いて同縮尺であることを示す。
- 高さと前後は**対で扱う**。片方だけ欠けている球を混ぜると2つの SD の n が揃わず、
  散布図の点と数値が 食い違う。`releasePairs()` で両方そろった球だけを集める。
- 全投球は薄く、直近10球は明るく、今回の投球は輪で囲んで描き分ける。
  直近10球の平均 ±1SD を破線の矩形で重ねる。

### F. 再現性・変化

全ての数値指標について下記を算出する（設計仕様書 §9-2、§10）。

| 名称 | 定義 |
|---|---|
| `mean_all` | 本人の全投球平均（信頼度 < 0.50 と未確定イベントの投球を除外） |
| `sd_all` | 同 標準偏差（不偏。n ≥ 3 のときのみ） |
| `mean_recent10` | 直近10球の平均（セッションを跨ぐ。使用したセッション数を併記） |
| `delta` | `今回値 − mean_all` |
| `delta_recent` | `今回値 − mean_recent10` |
| `n` | 集計に使った投球数（常に併記） |

**主表示は `delta_recent`**（設計仕様書 §10）。`mean_all` は副表示。

**ベースライン成熟度（実装時に追加）**

本書には n がいくつから比較してよいかの規定が無かったが、1〜2球を土台に「本人比」を出しても
意味が無いため、打撃版（`batting-v1a-measurement-spec.md` §6-2）と同じ段階を採る。

| n（イベント確定済みの投球数） | 扱い |
|---|---|
| n < 5 | **比較もアラートも出さない。**「データ蓄積中（あと◯球）」と表示 |
| 5 ≤ n < 10 | 参考値として比較。「参考」ラベルを付ける |
| n ≥ 10 | 通常比較 |

**リリース位置のばらつき（§8-2 の `リリース位置 SD` の定義）**

高さと前後の2軸あるため、スカラー1つに畳む。

```
spread = √( SD(rel_height)² + SD(rel_ext)² )
比 = spread(直近10球) / spread(全投球)
```

両方の窓で n ≥ 3 を満たさないときは算出せず、アラートも出さない。

---

## 6. 保存データスキーマ

### 6-1. 投球レコード（1球1件）

保存先は **IndexedDB**（決定事項 §1-1 の6）。

```
Pitch {
  id, session_id, pitch_no, timestamp
  video_ref, fps, video_ar
  pitcher_id
  calib:   { k_nose, calibrated:bool, H_norm, cm_per_unit|null, dir, dir_manual:bool,
             capture_fps|null, slow_factor }
  events:  { set, lift, fp, arm, rel, fin }        // 各 { frame, t_sec, confirmed:bool }
  metrics: { speed_kmh,
             stride_ratio, stride_cm,
             pelvis_disp, pelvis_vel,
             knee_fp, knee_rel, knee_ext, elbow_fp, trunk_fp, trunk_rel,
             t_fp_arm, t_fp_rel, t_arm_rel,
             rel_height, rel_ext }
  landmarks: { set:[33×{x,y,vis}], lift:[...], fp:[...], arm:[...], rel:[...] }
  quality:   { conf:{ stride, pelvis, knee_fp, elbow, release },
               checklist:{ full_body, brightness, camera_level, camera_side },
               fps_warning:bool }
  note
}

Pitcher { id, name, height_cm, throws:"R"|"L", k_nose, calibrated:bool }
Session { id, pitcher_id, date, place, started_at, ended_at, note, pitch_ids[] }
```

**イベントフレームのランドマークを丸ごと保存する。**
これにより、`k_nose` の較正・指標定義の変更・投球腕の指定ミスの修正があっても、
動画を読み直さずに全投球を再計算できる。

概算：1球 5イベント × 33点 × 3値 ≈ 500数値 ＝ JSON で 6〜8KB。100球で 0.6〜0.8MB。

### 6-2. 撮影チェックリスト（設計仕様書 §12-1）

セッション開始時に4項目をユーザーが確認してチェックする（自動判定はしない）。

```
□ 全身（頭〜足先）が画角に入っている
□ 極端に暗くない／逆光でない
□ カメラが水平（傾いていない）
□ 投手の真横から撮っている
```

チェックの有無を `quality.checklist` に保存し、未チェック項目があるセッションには
ダッシュボードで注記を出す。

---

## 7. CSV エクスポート（設計仕様書 §13・Ver.1 必須要件）

**1球1行**。ヘッダは固定順・英字キー。UTF-8（BOM 付き）。欠測は空欄（`0` を入れない）。数値は小数第3位まで。

```
date, session_id, pitch_no, pitcher_name, height_cm, throws, fps, capture_fps, slow_factor, calibrated,
speed_kmh,
stride_ratio, stride_cm,
pelvis_disp, pelvis_vel,
knee_fp, knee_rel, knee_ext, elbow_fp, trunk_fp, trunk_rel,
t_fp_arm_ms, t_fp_rel_ms, t_arm_rel_ms,
rel_height_ratio, rel_height_cm, rel_ext_ratio, rel_ext_cm,
conf_stride, conf_pelvis, conf_knee_fp, conf_elbow, conf_release,
vis_ankle_lead, vis_ankle_pivot, vis_toe_pivot, vis_wrist_throw, vis_elbow_throw, vis_hip_l, vis_hip_r,
（各 `vis_*` は保存した全イベントフレームのうち **最も低い visibility**。いちばん厳しい側を残す）
event_fp_confirmed, event_arm_confirmed, event_rel_confirmed,
fps_warning, note
```

外部送信は行わない（設計仕様書 §15）。ローカルにファイルを生成するのみ。
併せて **セッション単位の JSON エクスポート／インポート**を実装する（バックアップ・端末移行用）。

---

## 7-B. キネマティック・シークエンス（運動連鎖）

**設計仕様書 §6 に対応。Ver.1.1 で定義を作り直した。**

### 7-B-1. 旧定義を捨てた理由

初版の実装は、骨盤線・肩線の**画像上の傾き**（左右の高さの差）の変化速度を「角速度」としていた。
これは誤りである。**キネマティック・シークエンスが定義されているのは鉛直軸まわりの回旋**であり、
側面カメラでは体が回っても画像上の傾きはほとんど変わらない。傾きの変化は主に側屈（obliquity）を
拾っており、回旋の代用にすらなっていない。「体幹」として出していた量も体幹の**前後傾**で、回旋ではなかった。

### 7-B-2. 新定義：見かけの幅の短縮から回旋角を復元する

体が鉛直軸まわりに角 φ だけ回ると、左右のランドマーク間の**見かけの幅が cos φ で縮む**。

```
W(t)   = |X_左 − X_右|                （等方座標での見かけの幅）
W_max  = 解析区間内の W の95パーセンタイル   （φ ≈ 0、体が画像面と平行なときの実幅）
φ(t)   = arccos( clamp( W(t)/W_max, 0, 1 ) )        [度]
ω(t)   = |dφ/dt|                       （中心差分・実時間）        [度/秒]
```

| 部位 | 使うランドマーク |
|---|---|
| **骨盤（Pelvis）** | 左股関節 23 / 右股関節 24 |
| **胸郭（Trunk / Thorax）** | 左肩 11 / 右肩 12 |
| **上腕（Arm）** | 肩→肘 の**画面内**角度。2D |
| **前腕・手（Hand）** | 肘→手首 の**画面内**角度。2D |

`W_max` に最大値ではなく95パーセンタイルを使うのは、検出の外れ値1枚で実幅が過大に決まると
全区間の φ が過小になるため。

### 7-B-3. 復元が使えない条件（実装で明示的に除外する）

`dφ/dW ∝ 1/sin φ` なので、**φ が小さい（体が画像面と平行に近い）ほど誤差が増幅される。**

```
W(t)/W_max > 0.995   （φ < 約5.7°）のフレームは角速度を出さない
```

除外したフレーム数を画面に表示する。黙って穴を空けない。

### 7-B-4. 解析区間

**最大脚上げ 〜 リリース**。リリース以降は回旋が 90° を越え、見かけの幅が再び広がるため
`arccos` が折り返して回旋角が減少したように見える。

### 7-B-5. 出力

| 項目 | 内容 |
|---|---|
| ピーク角速度 | [度/秒]。**実数値をそのまま表示**（縦軸に単位のあるグラフ）。各部位の最大で割った正規化表示にも切り替えられる |
| ピーク時刻 | [s] |
| 前の部位との差 | [ms]。負なら逆順 |
| ピーク後の最大減速 | [度/秒²]。**ブレーキの鋭さ** |
| 隣接部位の関係 | 前の部位が**減速している最中に**次がピークを迎えたか（次への受け渡しが起きている向きか） |

### 7-B-6. 判定

`res = 1000/fps/slow_factor`（1コマの実時間）とする。

| 条件 | 判定 |
|---|---|
| いずれかの差が `≤ −res` | **逆順 N組** |
| いずれかの差が `|差| < res` | **同時 N組**（本当に同時か、fps 不足で分離できていないかは区別できない旨を併記） |
| すべて正で fps ≥ 120 | 順序 ✓ |
| すべて正で fps < 120 | 順序 ✓（参考） |

**fps は判定を一律に伏せる条件にはしない。** 実際のピーク間差は 20〜50ms 程度なので
30fps（1コマ 33ms）では小さな差を分離できないが、**1コマを超える逆転は 30fps でも読める**。
「120fps 以上で撮ること」は注意書きとして常に添える。

### 7-B-7. この数値の限界（UI に常時表示する）

- ⚠️ **上腕・前腕は画面内の2D角速度。**腕の動きは3次元なので、奥行き方向の成分は入らない。
- ⚠️ **研究で引用される絶対値（600〜1200 °/s 等）とは比較できない。**測り方が違う。
  比較対象は本人の過去の投球のみ。
- ⚠️ **ピーク角速度は平滑化のぶんだけ系統的に低く出る。**
  合成データでの実測で骨盤 −7%、手 −17%（速い部位ほど大きく低下）。
  ただし**ピーク時刻はほとんどずれない**（実測 0〜1コマ）ので、順番の判定には影響しない。
- 胸郭の回旋は肩線から求めているため、肩甲骨の動きと胸郭の回旋を分離していない。

### 7-B-8. 検証結果（合成データ）

回旋を「見かけの幅が cos で縮む」形で仕込んだスタブで確認した。

| 部位 | 仕込んだ理論値 | 独立実装（平滑化込み） | アプリ | 時刻差 |
|---|---|---|---|---|
| 骨盤 | 285.7 °/s @1.45s | 265 °/s @1.433s | 265 °/s @1.433s | −17ms |
| 胸郭 | 386.4 °/s @1.60s | 344 °/s @1.600s | 344 °/s @1.600s | 0ms |
| 上腕 | 666.7 °/s @1.75s | 577 °/s @1.733s | 577 °/s @1.733s | −17ms |
| 前腕 | 1071.4 °/s @1.90s | 893 °/s @1.900s | 893 °/s @1.900s | 0ms |

**アプリと独立実装の差は全部位 0.00%。** 理論値との差は平滑化と中心差分による減衰。
同時ピーク・逆順のスタブでも、それぞれ「同時 1組」「逆順 1組」と判定できることを確認した。

---

## 8. 比較とアラート

### 8-1. 表示の4階層（設計仕様書 §9-1）

画面上でも視覚的に分離し、「事実」と「示唆」を混同させない。

| 段階 | 画面上の扱い |
|---|---|
| ① Measurement | 数値をそのまま表示 |
| ② Comparison | `delta_recent` を主、`mean_all` を副として併記。n を必ず表示 |
| ③ Alert | レベルのみ（色とラベル）。文章にしない |
| ④ Interpretation | 事実記述の定型文のみ。断定しない |

### 8-2. アラート閾値（確定値）

比較対象は `delta_recent`（直近10球平均との差）。全項目を設定画面から変更可能とする。

| 指標 | Level 1 | Level 2 |
|---|---|---|
| 球速 | −2.0 km/h | −4.0 km/h |
| ストライド身長比 | ±3 pt | ±6 pt |
| 骨盤中心移動速度 | −10% | −20% |
| FP 時前膝角度 | ±5° | ±10° |
| 前膝伸展量 | −5° | −10°（`knee_ext ≤ 0` は無条件で Level 1） |
| FP→リリース時間 | +10 ms | +20 ms |
| リリース高さ（身長比） | ±0.015 | ±0.030 |
| リリース前後位置（身長比） | ±0.015 | ±0.030 |
| リリース位置 SD（直近10球 vs 全体） | 1.5倍 | 2.0倍 |

**Level 3（複合）**：Level 2 が同時に2項目以上、または
`球速 Level1以上の低下` かつ `リリース位置SD 増加` かつ `骨盤中心移動速度 Level1以上の低下`
の3点同時 → 設計仕様書 §12「疲労に伴う動作変化の可能性」として表示。

⚠️ **これらは外部から与えた初期値であり、その選手にとって適切である保証はない。**
設計仕様書 §1 原則1・2 に従えば閾値は本来その選手のデータから決めるべきもの。
30球程度たまった時点で本人の SD を見て設定画面から再設定する運用を前提とする。
SD からの自動設定は Ver.1.5（設計仕様書 §16-1）。

### 8-3. 疲労解析（設計仕様書 §12）

セッション内で `pitch_no` 昇順に10球ずつブロック化し、ブロック平均を並べる。
比較する指標：球速・`stride_ratio`・`rel_height`・`rel_ext`・`knee_fp`・`t_fp_rel`。
球速低下とリリース位置 SD 増加が同時に見られた場合、Level 3 として表示する。

---

## 9. ドリル候補 — **実装しない（方針変更）**

前版では `Drill` / `DrillRule` を持ち、ユーザーがドリルを登録して条件に紐付ける
「仕組みだけ」を提供する方針だった。**これを取りやめ、機能ごと削除する。**

理由：

- 本ソフトの役目は**測って、本人の過去と比べて、違いを示すところまで**である。
  そこから先の「では何をするか」は指導者の領域で、システムが場所を用意するだけでも
  「ここに書かれた練習をすればよい」という受け取られ方をしうる。
- 空の入れ物を置いておくと、埋めるべきものがあるように見える。
  作成者は投球指導の専門家ではないため、埋め方の良し悪しも示せない。

これに伴い **§8-1 の表示階層は5段から4段（Measurement → Comparison → Alert → Interpretation）**とする。
付録 B のドリル例も参考情報として残すが、システムからは参照しない。

---

## 10. 画面構成（設計仕様書 §14）

| 画面 | 内容 |
|---|---|
| 投球概要 | 投球数／平均・最大球速／平均リリース位置。**イベント未確定の球は平均から除き、除いた球数を明記する**（実装時に確定） |
| Mechanics | ストライド・前膝・肘・体幹（現在値 vs `mean_recent10`、Δ表示） |
| Timing | FP→Arm Peak→リリースのタイムライン |
| Consistency | リリース位置 SD と散布図（§5 E-3）、ストライド SD、球速 SD |
| 比較 | 保存済みの2球を選び、イベントのコマの骨格を重ねて表示＋指標の差（実装時に追加） |
| Fatigue | 10球ブロックごとの推移グラフ |

**推移グラフの描画要件（実装時に確定）**

比較する6指標は単位がばらばら（km/h・%・度・ms・身長比）なので、
**第1ブロックからの変化率 [%]** に揃えて1枚に重ねる。絶対値は同じ画面の表で見る。

- 0% の基準線を太く引き、上下に振れた向きがそのまま増減になるようにする。
- **第1ブロックの平均が 0 付近の指標は系列ごと外す**（変化率が発散するため）。
  外したことは凡例に明示し、黙って消さない。
- 2ブロック（20球）未満では推移を描かない。1点では傾きが無い。
| Data Quality | ランドマーク信頼度・撮影チェックリスト・fps 警告 |
| 動画オーバーレイ | 骨格・軌跡・角度数値・イベントラベルの重畳表示 |


### 保存済みレコードどうしの比較（実装時に追加）

イベントのコマの全ランドマークを保存してあるので、**動画がもう手元になくても**2件を重ねて比べられる。

- 2件とも**それぞれの `H_norm` で割ってから**重ねる。撮影距離やカメラ位置が違っても比べられる。
- 重ねる基準点は**両足首の中点（地面に近い）／股関節中点／肩中点**から選ぶ。
  足首基準は全身の位置関係、股関節基準は手足の使い方が見やすい。
- **縦横は同じ縮尺**で描く（単位が同じ身長比なので、軸ごとに変えると形が歪む）。
  2体を囲む枠が画面いっぱいになるように収める。
- ⚠️ **カメラの角度が違うと形そのものが変わる。**これは正規化では補正できないので、
  同じ位置から撮った動画どうしで比べる旨を画面に明記する。
- 指標は A / B / **B − A** の3列で並べる。どちらが良いかは判定しない。

---

---

## 11. 既存アプリからの差分と実装順

新規ファイル `web/pitching-analyzer.html`（決定事項 §1-1 の7）。

### 現行 `formanalyzer.html` から再利用できるもの

動画読込・MediaPipe 初期化とフォールバック・骨格オーバーレイ（描画矩形の位置合わせ済み）・
0.1/0.01秒/1コマのコマ送り・トリミング・シークバー・実 fps 計測・フレーム単位の解析キャッシュ・
グラフ描画基盤・CSV 出力基盤・オフライン時の劣化動作・
イベントフレームのランドマークを保存する設計（現行の「試技」保存とほぼ同じ）。

### 新規または作り直しが必要なもの

| 項目 | 内容 |
|---|---|
| 平滑化パイプライン | §2-2。現行は生座標をそのまま使用 |
| 較正と身長比スケール | §2-4。現行は胴体長比のみ |
| イベント | 6点（`set/lift/fp/arm/rel/fin`）＋候補±5コマのサムネイル確定 UI |
| コマ送り | §4-0。実フレーム時刻基準の1コマ送り＋0.001秒微調整（**現行 `formanalyzer.html` に実装済み。移植する**） |
| スロー補正 | §2-5b。`capture_fps` 設定と実時間換算（**現行 `formanalyzer.html` に実装済み。移植する**） |
| iOS 対応 | タッチ端末で最小タップ領域44px、データ書き出しの導線（**現行に実装済み。移植する**） |
| 指標 | §5 の A〜F に総入れ替え |
| 目安値表示 | 撤去（決定事項 §1-1 の8） |
| データストア | §6。IndexedDB・選手・セッション・球速入力・JSON 入出力 |
| 信頼度の保存と表示 | §2-5。現行は閾値で捨てるだけで値を保存していない |
| アラートエンジン | §8。全て新規 |
| ダッシュボード | §10 の7面 |
| CSV | §7 の1球1行形式 |

### 実装順（設計仕様書 §17 に対応）

```
Phase 1  再利用部分の切り出し ＋ 平滑化 ＋ 較正・身長比スケール ＋ 選手/セッション管理   ← 実装済み
Phase 2  イベント6点の候補生成 ＋ サムネイル確定 UI   ← 実装済み
Phase 3  §5 A〜E の指標算出   ← 実装済み
Phase 4  タイミングとキネマティック・シークエンス（§7-B）        ← 実装済み
Phase 5  複数球の平均・SD・Δ（§5-F）と §8-1 の4階層表示
Phase 5.5 CSV／JSON 入出力（§7）＋ 信頼度（§2-5）＋ 撮影チェックリスト（§6-2）
Phase 6  アラート（§8-2）＋ 疲労解析（§8-3）
```

---

## 12. Ver.1 で実装しないこと（設計仕様書 §16 の再掲・確定）

カメラ自動同期／3D 解析・三角測量／Hip-Shoulder Separation・回旋角速度の絶対値評価／
球種間ピッチトンネル比較／LLM による自然文講評／医療診断・故障確率予測・プロ選手との単純比較／
相関分析・ベスト投球分析・個人最適ゾーン・Z-score（設計仕様書 §16-1 の通り Ver.1.5 以降）。

---

## 付録 A. 参考文献の目安値（システムでは使用しない）

設計仕様書 §8 の通り、以下は**読み物**であり、判定・アラート・画面表示のいずれにも使わない。
光学式モーションキャプチャ（数百fps・マーカー式）を前提とした値であり、
スマホ動画＋2D 姿勢推定の値と直接比較すること自体が誤解を招く。

- 前足接地時：肘屈曲 約90°／前膝屈曲 約45°
- ストライド長：身長の 80〜90% 程度
- Hip-Shoulder Separation：20〜50° 程度とする報告が多い（Ver.1 では後方カメラがないため算出も表示もしない）

## 付録 B. ドリル候補のサンプル（**システムからは参照しない読み物**）

⚠️ **これは仕組みの動作確認用のサンプルであり、指導内容としての推奨ではない。**
作成者は投球指導の専門家ではない。使用する場合は指導者ご自身の判断で全面的に差し替えること。

| アラート条件（事実） | 解釈（事実記述） | ドリル候補群 |
|---|---|---|
| `knee_ext ≤ 0` ／伸展量が本人平均より低下 | 前足接地後の下肢動作に変化がある可能性 | 前脚ブロック系ドリル／メディシンボール投げ／片脚立位保持 |
| `stride_ratio` が本人平均より低下 | 踏み出しが小さくなっている | ストライド長を目印で固定した投球／下肢柔軟性の確認 |
| `pelvis_vel` が低下 | 前方への並進が弱い可能性 | ステップドリル／体重移動の確認 |
| `t_fp_rel` が延長 | 接地からリリースまでが長くなっている | テンポを規定したシャドウ／メトロノーム下での投球 |
| リリース位置 SD 増加 | リリースの再現性が落ちている | 的当て／短い距離でのフォーム確認 |
| Level 3（複合） | 疲労に伴う動作変化の可能性 | ドリルではなく**休息・投球数の見直し**を検討 |
